# jQuery Disabler

This tiny plugin makes it easy to disable any input controls or anchor tags.

		// Call disable on a selector and recursively disable
		// all input controls and anchor tags
		$(selector).disable();

		// Call enable to enable 
		$(selector).enable();